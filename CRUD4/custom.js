function myFunction(value) {
   
    // If x is Not a Number or less than one or greater than 10
    if (isNaN(value.bo_cash) || value.bo_cash < 1) {
        text = "Input not valid";
    } else {
        text = "Input OK";
    }
}